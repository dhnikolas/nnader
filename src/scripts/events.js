/**
 * Клас - шина событий. На этот компонент вешаются события и слушаются снего.
 * Таким образом любой компанент приложения может взаимодействовать с любым другим.
 */
import Vue from 'vue';
export const Events = new Vue();